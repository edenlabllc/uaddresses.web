import { schema } from 'normalizr';

export const region = new schema.Entity('regions');
export const district = new schema.Entity('districts');
export const settlement = new schema.Entity('settlements');
export const street = new schema.Entity('streets');
